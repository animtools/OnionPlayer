# Changelog — OnionPlayer

バージョンごとの変更内容です。

---

## [0.1.1] — 2026-08-26

### 修正

- **ML モデルの導入に進めないことがある問題を直しました。**
  インストール直後の状態から抽出方式を ML に切り替えたとき、モデルの導入案内に
  たどり着けない場合がありました。

- **ライセンスの認証枠を余分に消費していた問題を直しました。**
  これまでは同じ PC でもアプリごとに1枠を使っていたため、スイートのアプリを3つ入れると
  上限に達していました。今後は **PC 1台につき1枠**です。
  すでに認証済みの端末はそのまま引き継がれるので、認証をやり直す必要はありません。

- **ライセンスの認証が、スイート全体で共有されるようになりました。**
  今後スイートに他のアプリが加わったとき、同じ PC ならキーの再入力なしで使えます。
  個別アプリの買い切りライセンスは、これまでどおりそのアプリだけを解除します。

- **入力したライセンスキーが、入力欄の履歴に残らないようにしました。**
  以前は入力欄を選ぶと過去に入れたキーが候補として表示されることがありました。

---

## [0.1.0] — 2026-08-23（テスト配布）

初回のテスト配布版です。

### 線画抽出

- **シェーダ抽出**（Sobel / DoG / XDoG）— GPU で数ミリ秒。再生中も動きます。
- **ML 抽出**（Lineart / OpenSketch）— より綺麗な線。モデルは初回にアプリ内からダウンロード。
  GPU（DirectML）で動作し、使えない環境では CPU にフォールバックします。
- **線の出方**の調整（黒レベルで足切りしてから線の強さを掛ける）。光線やグローのような
  軟らかい階調のエッジも線として拾えます。
- 再生中に ML が追いつかない区間は**シェーダ抽出へ自動フォールバック**（状態を画面に表示）。
  停止・コマ送り時は常に ML 品質に戻ります。

### オニオンスキン

- 過去・未来の層を任意の数・間隔で重ねて表示。
- **オニオンイコライザ** — 層ごとの濃さを縦棒で直接調整。プリセットとして保存できます。
- **キーフレーム基準** — 打ったキーフレームの区間を等分して層を配置。等間隔（コマ数基準）
  との切り替えが可能です。
- 減衰ランプ・個別上書き・着色強度の調整。
- **重ねて表示 / 並べて表示**（独立したペインに並べる）。
- **めくり**（層をコマ送りでめくる）と**パラパラ**（指定 FPS で自動送り）。

### 表示・操作

- **静止部の除去** — 背景モデルと輝度が一致する画素の線を落とし、動いたものだけを残します
  （固定カメラ素材向け・既定 OFF）。
- **方眼**オーバーレイ、**集中モード**、ズーム・パン。
- 区間ループ・コマ送り・秒送り・スクラブ・ピーク操作。

### 書き出し（Pro）

- PNG の保存・クリップボードへのコピー・**層ごとの書き出し**。

### ライセンス

- フリーミアム（無料 + Pro）。Pro は **ML 抽出**と**書き出し**を解放します。
- 認証情報は Windows の資格情報マネージャーに保存されます。
- オフライン猶予7日・再検証間隔24時間。判定はローカルで完結し、常時接続を必要としません。

### 保存

- 設定・キーフレーム・ML モデルは `OnionPlayer.exe` と同じフォルダの `OnionPlayerData\` へ。
  **フォルダごと持ち運べます**（ライセンス認証状態を除く）。

### 既知の制限

- **購入導線は未開通**です（テスト配布中のため）。アプリ内の購入ボタンは案内ページへ
  つながりますが、まだ購入できません。
- コード署名がないため、初回起動時に SmartScreen の警告が出ます。
- 静止部の除去はカメラが動く素材には効きません（固定カメラ前提）。
