# SUPPORT — OnionPlayer

不具合報告・質問・要望・感想をお待ちしています。

---

## 連絡方法

- **メール**: **shinosuke.support@gmail.com**

どんな細かいことでも構いません。**「ここが要る」「これだと回らない」という話が一番役に立ちます。**
うまくいった話より、詰まった話のほうが助かります。

## 不具合報告のときに書いていただきたいこと

スムーズに調査できるよう、可能な範囲で以下を添えてください。

1. **現象** — 何が起きたか（期待した動作と、実際の動作）
2. **再現手順** — どの操作で起きるか（1. …→ 2. …→ 3. …）
3. **バージョンとビルド時刻** — **設定モーダル**に表示されています。そのまま貼り付けてください。
   **古い実行ファイルが残っていて起きている不具合が実際にあります**ので、これが一番効きます。
4. **環境** — Windows のバージョン、GPU（ML 抽出の問題の場合）
5. **素材の情報** — 解像度・フレームレート・コーデック・長さ（再生や抽出の問題の切り分けに要ります）
6. **スクリーンショット** — あれば添付してください（画面の状態が分かると助かります）

## よくある質問

- **データはどこに保存されますか？** — `OnionPlayer.exe` と**同じフォルダの `OnionPlayerData\`** に、
  設定・キーフレーム・ML モデルが保存されます。フォルダごと持ち運べます。クラウドへ自動送信はしません。
- **読み込んだ動画はどこかへ送られますか？** — いいえ。線画抽出も合成も、すべてお使いの端末上で
  完結します。
- **別のパソコンへ移したらライセンスが外れました** — 仕様です。設定やキーフレームはフォルダごと
  付いてきますが、**認証状態だけは付いてきません**。移した先でもう一度認証してください。
- **ML 抽出が「モデルが未インストール」と出ます** — モデル（約 241 MB）は初回にアプリ内から
  ダウンロードします。設定モーダルから実行してください。
- **ML 抽出が遅い／GPU が使われていない気がします** — GPU（DirectML）が使えない環境では
  自動的に CPU にフォールバックします。動作はしますが、1コマあたりの時間は長くなります。
- **再生中に線が粗くなります** — 追いつかない区間はシェーダ抽出へ自動で切り替わる設計です
  （画面に表示が出ます）。停止・コマ送りのときは常に ML の品質に戻ります。
- **静止部の除去で背景が消えません** — 固定カメラの素材を前提にした機能です。カメラが動く
  素材では、背景も「動いたもの」として残ります。
- **起動時に SmartScreen の警告が出ます** — 未署名アプリのためです。
  [QUICKSTART.md](./QUICKSTART.md) の案内を参照してください。
